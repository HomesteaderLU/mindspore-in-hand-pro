apply plugin: 'com.android.library'

android {
    compileSdkVersion 30

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 30
    }
}

dependencies {
    // 将预编译的 AAR 作为 API 依赖暴露给消费方模块
    api files('mindspore/mindspore-lite/1.1.0/mindspore-lite-1.1.0.aar')
}